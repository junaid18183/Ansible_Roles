# config for templated.Catalog, using camelCase because I don't want to do
# translations for keystone compat
catalog.RegionOne.identity.publicURL = http://{{ controller_ip }}:$(public_port)s/v2.0
catalog.RegionOne.identity.adminURL = http://{{ controller_ip }}:$(admin_port)s/v2.0
catalog.RegionOne.identity.internalURL = http://{{ controller_ip }}:$(public_port)s/v2.0
catalog.RegionOne.identity.name = Identity Service

# fake compute service for now to help novaclient tests work
catalog.RegionOne.compute.publicURL = http://{{ controller_ip }}:8774/v1.1/$(tenant_id)s
catalog.RegionOne.compute.adminURL = http://{{ controller_ip }}:8774/v1.1/$(tenant_id)s
catalog.RegionOne.compute.internalURL = http://{{ controller_ip }}:8774/v1.1/$(tenant_id)s
catalog.RegionOne.compute.name = Compute Service

catalog.RegionOne.volume.publicURL = http://{{ controller_ip }}:8776/v1/$(tenant_id)s
catalog.RegionOne.volume.adminURL = http://{{ controller_ip }}:8776/v1/$(tenant_id)s
catalog.RegionOne.volume.internalURL = http://{{ controller_ip }}:8776/v1/$(tenant_id)s
catalog.RegionOne.volume.name = Volume Service

catalog.RegionOne.ec2.publicURL = http://{{ controller_ip }}:8773/services/Cloud
catalog.RegionOne.ec2.adminURL = http://{{ controller_ip }}:8773/services/Admin
catalog.RegionOne.ec2.internalURL = http://{{ controller_ip }}:8773/services/Cloud
catalog.RegionOne.ec2.name = EC2 Service

catalog.RegionOne.image.publicURL = http://{{ controller_ip }}:9292/v1
catalog.RegionOne.image.adminURL = http://{{ controller_ip }}:9292/v1
catalog.RegionOne.image.internalURL = http://{{ controller_ip }}:9292/v1
catalog.RegionOne.image.name = Image Service

catalog.RegionOne.network.publicURL = http://{{ controller_ip }}:9696
catalog.RegionOne.network.adminURL = http://{{ controller_ip }}:9696
catalog.RegionOne.network.internalURL = http://{{ controller_ip }}:9696
catalog.RegionOne.network.name = OpenStack Networking
